export const getters = {
  jobs: (state) => state.jobs,
  applications: (state) => state.applications,
  isJobSelected: (state) => state.isJobSelected,
  user: (state) => state.user,
  isUserLogged: (state) => state.isUserLogged
}
